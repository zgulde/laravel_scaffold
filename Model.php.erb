<?php

namespace App;

use Illuminate\Database\Eloquent\Model;

class <%= resource.capitalize %> extends Model
{
    public $rules = [
        <% attributes.each do |name, type| %>'<%= name %>' => '',
        <% end %>
    ];

    protected $fillable = ['<%= attributes.keys.join("', '") %>'];
}
