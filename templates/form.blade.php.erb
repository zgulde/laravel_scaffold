<% attributes.each do |a| %>
 <%= render("templates/partials/#{a[:type] == 'text' ? 'textarea' : 'input'}.blade.php.erb", a, resource) %>
<% end %>
<input class="btn btn-block btn-primary" type="submit">
